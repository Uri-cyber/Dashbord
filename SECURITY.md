# אבטחה (SECURITY)

מסמך זה מרכז הנחיות אבטחה, הקשחות שבוצעו, וסיכונים ידועים עבור הפרויקט.

## רקע קצר
האפליקציה היא דף יחיד (SPA) ב־HTML/JS שקורא נתונים מ־`projects.json` ומ־`localStorage`, ומאפשר הוספה/עריכה/מחיקה של פרויקטים. מאחר שקלט יכול להגיע מקובץ חיצוני או מהמשתמש, קיימים סיכוני XSS והזרקת HTML אם לא מבצעים סינון נכון לפני הזרקה ל־DOM.

## הקשחות שבוצעו (2025‑09‑29)
- נוספה פונקציה `escapeHTML(str)` שמסננת תווים מיוחדים (`& < > " '`) לפני הזרקה ל־DOM.
- `truncateText` הותאמה בעדינות כך שהיא מחזירה טקסט מסונן כברירת מחדל, בלי לשנות את חוויית המשתמש.
- במודל העריכה: ערכי `value` מוזנים מסוננים (`escapeHTML`) כדי למנוע שבירת HTML/JS.
- בחלון ההיסטוריה: `date`, `type`, `description` עוברים סינון לפני הזרקה ל־`innerHTML`.
- כותרות הכרטיסים (`title`) מוגדרות דרך DOM API לאחר הרינדור במקום הזרקה ישירה לתבנית.

## הנחיות פיתוח בטוח
- לא מזריקים מחרוזות לא מהימנות ל־`innerHTML`. במקום זאת:
  - יוצרים אלמנטים עם `document.createElement` ומשתמשים ב־`textContent`/`setAttribute`.
  - אם חייבים תבנית, לעטוף ערכים ב־`escapeHTML` מראש.
- הימנעות מהזרקת HTML לתכונות כמו `href/src/style/on*`. אם מוכרחים, לאמת פורמט ולבצע allowlist לפרוטוקולים (`http:`, `https:`) באמצעות `new URL()`.
- לא מבצעים `eval`/`Function(...)` על נתונים דינמיים.
- `localStorage` אינו מקור מהימן. יש להתייחס לכל נתון ממנו כלא מהימן ולסנן לפני הצגה.
- מומלץ ולא חובה: ולידציה סכמטית בסיסית ל־`projects.json` (שדות נדרשים, סוגי נתונים, מגבלות אורך).

## בדיקות אבטחה ידניות
נסה להזין בשדות/שם פרויקט מחרוזות כגון:
- `<script>alert(1)</script>`
- `<img src=x onerror=alert(2)>`
- `" onmouseover="alert(3)`
- תווים מיוחדים: `& < > " '` 

התוצאה המצופה: כל הטקסט מוצג כפי שהוא ולא מבוצעת הרצת קוד. בנוסף, תכונת `title` בכרטיס מוצגת בטוח ואינה שוברת את ה־HTML.

## מגבלות ידועות ומעקפים מומלצים
- בדיקת סטטוס שירותים מתבצעת עם `fetch(..., { mode: "no-cors" })`, ולכן התגובה אטומה ולא ניתן להסיק קוד סטטוס אמיתי. אפשרויות:
  - להשתמש בפרוקסי/שרת ביניים שמבצע את הבדיקה בצד השרת ומחזיר קוד סטטוס.
  - או לסמן תשובה אטומה כ"לא ידוע" במקום "פעיל" כדי לא להטעות משתמשים.
- שקול הוספת timeouts ו־retry לבדיקות רשת כדי לשפר UX.

## דיווח על בעיות אבטחה
נא לפתוח Issue פרטי או לפנות למתחזק/ת הפרויקט. אם תרצה, אפשר להוסיף כאן אימייל/ערוץ קשר ייעודי.

